{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd .system/RetrivalSystem && python manage.py migrate && py manage.py collectstatic --noinput && gunicorn RetrivalSystem.wsgi",
        "restartPolicyType": "NEVER",
        "restartPolicyMaxRetries": 0
    }
}
